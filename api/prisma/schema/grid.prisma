model Grid {
  id         String   @id @default(uuid()) @db.Uuid
  grid       Json     @unique
  solution   Json
  difficulty Int
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
